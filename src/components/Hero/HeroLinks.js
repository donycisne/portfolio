import React from 'react';

import SocialLinks from '../Icons/SocialLinks';

const HeroLinks = () => {
  return (
    <div className="hero-links" id="social-links">
      <SocialLinks />
    </div>
  );
};

export default HeroLinks;
